#!/usr/bin/env node

/**
 * Module dependencies.
 */
var path = require('path');
var fs = require('fs');
var lib = path.dirname(fs.realpathSync(__filename));
require(path.resolve(lib,'../lib/cli/command')).run();